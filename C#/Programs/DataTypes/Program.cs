namespace DataTypes
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // Text data types
            string str = "Tutorial";
            char ch = 'a';

            //Numeric types
            int num1 = 10;
            int num2 = -10;
            float fl = 9.9F;
            double db = 0.8;
            decimal dc = 3.44M;

            //Boolean
            bool isSunday = true;

            //constant
            Console.WriteLine("400");
        }
    }
}
/*
 * 1. There are 3 decimal number data types: float, double and decimal. float having least precicion
 *    and decimal having most precision.
 *    
 * 2. F is appended for a float variable, M is appended for a decimal variable.
 */